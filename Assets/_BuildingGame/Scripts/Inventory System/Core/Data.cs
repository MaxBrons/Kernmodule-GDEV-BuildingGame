namespace BuildingGame.Inventory
{
    // Default interface for scriptable objects so
    // you can reference a scriptable object without
    // specificly referencing the (sub)class type.
    public interface IData
    {

    }
}